#!/bin/bash

# 変数内の文字列を置換する
command=$1
if [ -z $ "$command" ]; then
  echo "comand does not exist" >&2
  exit 1
fi

for dir in ${PATH://}
do
  if [ -f "${dir}/${command}" ]; then
    echo "${dir}/${command}"
  if
done

